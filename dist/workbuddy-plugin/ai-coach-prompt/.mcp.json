{
  "mcpServers": {
    "ai-coach-profile": {
      "command": "node",
      "args": [
        "${CODEBUDDY_PLUGIN_ROOT}/servers/profile-store.mjs"
      ],
      "env": {
        "AI_COACH_DATA_DIR": "${CODEBUDDY_PLUGIN_DATA}"
      }
    }
  }
}
