# Coze adapter

Upload or package this directory with `SKILL.md` at the root. Keep the core in Markdown, YAML, and UTF-8 text; do not require a desktop-client widget, local path, or shell command for the learner flow.

If a Coze-specific manifest becomes required, add it in this adapter only rather than changing the portable core.
